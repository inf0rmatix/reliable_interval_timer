name: reliable_interval_timer
description: A timer that produces ticks within a given interval, accurate to the millisecond.
version: 1.0.1
repository: https://github.com/1nf0rmatix/reliable_interval_timer
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.2 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
